<?php

/**
 *  @file
 *  Node template : default
 *  By default and by design, we want JUST the content
 *  Also note : when using Panels on node pages, this is not used (unless in "rendered node" pane)
 */

print render($content);
